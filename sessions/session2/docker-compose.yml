version: '3.8'

services:
  # ## Use this postgres service if not using session1's postgres
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: session1_postgres
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres
  #     POSTGRES_DB: retail_db
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U postgres"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5

  transform:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: session2_transform
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: retail_db
      DB_USER: postgres
      DB_PASSWORD: postgres
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ../code:/app/code
      - ../tests:/app/tests
      - pytest_coverage:/app/htmlcov

volumes:
  # postgres_data:
  pytest_coverage:
